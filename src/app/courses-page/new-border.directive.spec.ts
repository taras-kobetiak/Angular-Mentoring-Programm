import { NewBorderDirective } from './new-border.directive';

describe('NewBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new NeworderDirective();
    expect(directive).toBeTruthy();
  });
});
