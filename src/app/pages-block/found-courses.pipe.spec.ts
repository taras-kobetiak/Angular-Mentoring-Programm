import { FoundCoursesPipe } from './found-courses.pipe';

describe('FoundCoursesPipe', () => {
  it('create an instance', () => {
    const pipe = new FoundCoursesPipe();
    expect(pipe).toBeTruthy();
  });
});
